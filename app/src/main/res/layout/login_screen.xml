<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="10dp"
    android:background="@color/app_background"
    tools:context="ordanel.ednom.Login" >

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/inei"
        android:contentDescription="@string/inei_content_description"
        android:scaleType="centerCrop"
        android:layout_marginTop="30dp"
        android:layout_gravity="center" />

    <EditText
        android:id="@+id/edtPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:hint="@string/password_text_hint"
        android:inputType="textPassword"
        android:maxLength="6" />

    <Button
        android:id="@+id/btnLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:text="@string/button_login_text"
        android:background="@drawable/button_cr"
        android:textColor="@color/text_white"
        android:textStyle="bold"
        android:padding="10dp"
        android:onClick="initLogin" />

</LinearLayout>